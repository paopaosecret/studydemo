<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:aigestudio="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.aigestudio.wheelpicker.WheelPicker
        android:id="@+id/main_wheel_left"
        android:layout_width="100dp"
        android:layout_height="match_parent"
        android:background="#E5DEEB"
        aigestudio:wheel_indicator="false"
        aigestudio:wheel_indicator_color="#ff0000"
        aigestudio:wheel_indicator_size="1dp"
        aigestudio:wheel_atmospheric="true"
        aigestudio:wheel_curtain="true"
        aigestudio:wheel_curtain_color="#50ffffff"
        aigestudio:wheel_curved="true"
        aigestudio:wheel_cyclic="false"
        aigestudio:wheel_item_text_color="#FF0000"
        aigestudio:wheel_item_text_size="18sp"
        aigestudio:wheel_selected_item_text_color="#536D8A"/>

    <ListView
        android:id="@+id/lv_right"
        android:layout_marginLeft="20dp"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_weight="1">
    </ListView>

</LinearLayout>